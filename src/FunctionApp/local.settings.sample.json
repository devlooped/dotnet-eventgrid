{
  "IsEncrypted": false,
  "Values": {
    // The shared secret, which must match the one passed in as the url from the dotnet tool
    "AccessKey": "42",
    "FUNCTIONS_WORKER_RUNTIME": "dotnet",
    "AzureWebJobsStorage": "UseDevelopmentStorage=true",
    "AzureSignalRConnectionString": "[YOUR_AZURE_SIGNALR_SERVICE_CONNECTION_STRING]"
  }
}
